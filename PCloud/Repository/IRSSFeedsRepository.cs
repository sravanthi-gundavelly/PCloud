using PCloud.Models;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace PCloud.Repository
{
    public interface IRSSFeedsRepository
    {
        //IEnumerable<RSSFeeds> GetAllRSSFeeds();

        //RSSFeeds GetRSSFeedById(Guid Id);

        //void CreateRSSFeeds(RSSFeeds RSSFeed);

        //void UpdateRSSFeeds(Guid Id, RSSFeeds RSSFeeds);

        //void DeleteRSSFeed(Guid Id);


    }
}
